# Exemplo mínimo de dataset YOLO Detection.
# Substitua o caminho e as classes pelas definições reais do projeto.

path: /caminho/para/dataset
train: images/train
val: images/val
test: images/test

names:
  0: class_0
  1: class_1
